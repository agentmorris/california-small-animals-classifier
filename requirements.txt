# California Small Animals classifier — Python 3.12, Windows, conda env
# PyTorch installed from the CUDA 12.4 index:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
torch==2.6.0
torchvision==0.21.0
timm==1.0.27
lightning==2.6.5
torchmetrics==1.9.0
pillow==12.2.0
numpy==2.4.6
pandas==3.0.3
pyarrow==24.0.0
pulp==3.3.2          # ILP for the class-aware camera split
tqdm
